# マクマHP 更新自動化

素材は `/Users/rarabai/Documents/Codex/マクマHP更新素材/` に入れます。フォルダとファイル名は次で固定します。

| 入れる場所 | ファイル名 | 内容 |
| --- | --- | --- |
| `ジャケット/` | `作品名（曲名）.jpg` または `.png` | ジャケット画像 |
| `歌詞/` | `曲名.docx` | 歌詞 |
| `Song Notes/` | `曲名.docx` | Song Notes原稿 |

歌詞とSong Notesは同じ曲名でも、それぞれのフォルダに置けます。更新スクリプトはフォルダ名で用途を判別します。

更新対象を明確にしたい場合は、受け取りフォルダ直下に `更新内容.txt` を置きます。次のような短い文で十分です。

```text
アルバム「○○」を追加。ジャケットと歌詞は同梱。
配信情報・トラックリスト・リンクは調べて取得。
Song Notes「△△」も追加。
```

## 実行

普段は、デスクトップの **Makumaサイト更新.command** をダブルクリックします。ターミナルが開き、素材の確認・サイト更新・ビルドを実行します。問題がなければ、最後に「公開しますか？」と表示されます。

「あとで確認」を選ぶと、変更はローカルに残り、本番公開はされません。「公開する」を選ぶと、変更をcommitしGitHubへpushします。Cloudflare Pagesが本番サイトへ反映します。

素材を一度だけ処理する場合:

```sh
npm run update:once
```

素材フォルダを継続監視する場合:

```sh
npm run update:watch
```

監視中は15秒ごとにフォルダを確認し、素材が30秒間変化しなければ処理します。新しい素材を処理した記録は `.cache/` に保存するため、同じ素材を二重に処理しません。

更新準備とビルドが完了した素材は、受け取りフォルダ内の `処理済み/日時/` へ自動で移動します。次回は受け取りフォルダの直下へ新しい素材だけを入れてください。保管済みの素材は処理対象になりません。

スクリプトが行うのは、素材確認、配信情報の調査、サイト更新、ビルド、リンク確認、ローカル差分の作成までです。commit・push・本番公開は実行しません。変更内容とプレビューを確認してから、このチャットで「公開して」と伝えてください。

`MAKUMA_UPDATE_INBOX` 環境変数を指定すると、受け取りフォルダを一時的に変更できます。
