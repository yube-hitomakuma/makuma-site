# マクマHP 更新自動化

素材は `/Users/rarabai/Documents/Codex/マクマHP更新素材/` に入れます。画像、歌詞の `.txt` または `.docx`、Song Notesの原稿をそのまま置いてください。ファイル名は自由です。

更新対象を明確にしたい場合は、同じフォルダに `更新内容.txt` を置きます。次のような短い文で十分です。

```text
アルバム「○○」を追加。ジャケットと歌詞は同梱。
配信情報・トラックリスト・リンクは調べて取得。
Song Notes「△△」も追加。
```

## 実行

素材を一度だけ処理する場合:

```sh
npm run update:once
```

素材フォルダを継続監視する場合:

```sh
npm run update:watch
```

監視中は15秒ごとにフォルダを確認し、素材が30秒間変化しなければ処理します。新しい素材を処理した記録は `.cache/` に保存するため、同じ素材を二重に処理しません。

スクリプトが行うのは、素材確認、配信情報の調査、サイト更新、ビルド、リンク確認、ローカル差分の作成までです。commit・push・本番公開は実行しません。変更内容とプレビューを確認してから、このチャットで「公開して」と伝えてください。

`MAKUMA_UPDATE_INBOX` 環境変数を指定すると、受け取りフォルダを一時的に変更できます。
